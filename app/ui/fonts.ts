import { Inter, Outfit, Lusitana, Geist } from "next/font/google";

export const inter = Inter({ subsets: ["latin-ext"] });
export const outfit = Outfit({ subsets: ["latin"] });
export const lusitana = Lusitana({ weight: ["400", "700"], subsets: ["latin"] });
export const geist = Geist({ subsets: ["latin"] });
